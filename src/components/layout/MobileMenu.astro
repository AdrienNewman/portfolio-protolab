---
// Mobile Menu Component
---

<div class="nav-mobile" id="nav-mobile">
    <ul class="nav-mobile-menu">
        <li class="nav-mobile-item"><a href="#accueil" class="nav-mobile-link">Accueil</a></li>
        <li class="nav-mobile-item"><a href="#competences" class="nav-mobile-link">Compétences</a></li>
        <li class="nav-mobile-item"><a href="#profil" class="nav-mobile-link">Profil</a></li>
        <li class="nav-mobile-item"><a href="#projets" class="nav-mobile-link">Projets</a></li>
        <li class="nav-mobile-item"><a href="#documentation" class="nav-mobile-link">Documentation</a></li>
        <li class="nav-mobile-item"><a href="#contact" class="nav-mobile-link">Contact</a></li>
    </ul>
    <div class="nav-mobile-deco">Adrien Mercadier — TSSR 2025</div>
</div>

<style>
    .nav-mobile {
        position: fixed;
        top: 0;
        left: 0;
        width: 100%;
        height: 100vh;
        background: var(--black);
        z-index: 1001;
        display: flex;
        flex-direction: column;
        justify-content: center;
        align-items: center;
        opacity: 0;
        visibility: hidden;
        transition: all 0.4s cubic-bezier(0.16, 1, 0.3, 1);
    }

    .nav-mobile.active { opacity: 1; visibility: visible; }
    .nav-mobile-menu { list-style: none; text-align: center; }
    .nav-mobile-item { margin: 1.5rem 0; overflow: hidden; }

    .nav-mobile-link {
        display: inline-block;
        font-family: 'Bebas Neue', sans-serif;
        font-size: clamp(2.5rem, 10vw, 5rem);
        color: var(--white);
        text-decoration: none;
        transform: translateY(100%);
        transition: transform 0.5s cubic-bezier(0.16, 1, 0.3, 1), color 0.3s ease;
    }

    .nav-mobile.active .nav-mobile-link { transform: translateY(0); }
    .nav-mobile-item:nth-child(1) .nav-mobile-link { transition-delay: 0.1s; }
    .nav-mobile-item:nth-child(2) .nav-mobile-link { transition-delay: 0.15s; }
    .nav-mobile-item:nth-child(3) .nav-mobile-link { transition-delay: 0.2s; }
    .nav-mobile-item:nth-child(4) .nav-mobile-link { transition-delay: 0.25s; }
    .nav-mobile-item:nth-child(5) .nav-mobile-link { transition-delay: 0.3s; }
    .nav-mobile-item:nth-child(6) .nav-mobile-link { transition-delay: 0.35s; }
    .nav-mobile-link:hover { color: var(--neon-cyan); }

    .nav-mobile-link::before {
        content: attr(data-index);
        position: absolute;
        left: -2rem;
        top: 50%;
        transform: translateY(-50%);
        font-family: 'Space Mono', monospace;
        font-size: 0.875rem;
        color: var(--neon-cyan);
    }

    .nav-mobile-deco {
        position: absolute;
        bottom: 3rem;
        font-family: 'Space Mono', monospace;
        font-size: 0.7rem;
        color: var(--gray-light);
        letter-spacing: 0.2em;
        text-transform: uppercase;
        opacity: 0;
        transition: opacity 0.4s ease 0.4s;
    }

    .nav-mobile.active .nav-mobile-deco { opacity: 1; }
</style>
