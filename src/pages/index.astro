---
import BaseLayout from '../layouts/BaseLayout.astro';
import Navbar from '../components/layout/Navbar.astro';
import MobileMenu from '../components/layout/MobileMenu.astro';
import Hero from '../components/sections/Hero.astro';
import Skills from '../components/sections/Skills.astro';
import Profile from '../components/sections/Profile.astro';
import ProjectGallery from '../components/ui/ProjectGallery.astro';
import Documentation from '../components/sections/Documentation.astro';
import Contact from '../components/sections/Contact.astro';
import Footer from '../components/layout/Footer.astro';
import PreviewWindows from '../components/modals/PreviewWindows.astro';
import PreviewLinux from '../components/modals/PreviewLinux.astro';
import PreviewPaloalto from '../components/modals/PreviewPaloalto.astro';
import PreviewProxmox from '../components/modals/PreviewProxmox.astro';
import PreviewObservability from '../components/modals/PreviewObservability.astro';
import PreviewLLM from '../components/modals/PreviewLLM.astro';
import PreviewBackup from '../components/modals/PreviewBackup.astro';
import PreviewCloud from '../components/modals/PreviewCloud.astro';
import ModalWindows from '../components/modals/ModalWindows.astro';
import ModalLinux from '../components/modals/ModalLinux.astro';
import ModalPaloalto from '../components/modals/ModalPaloalto.astro';
import ModalProxmox from '../components/modals/ModalProxmox.astro';
import ModalObservability from '../components/modals/ModalObservability.astro';
import ModalLLM from '../components/modals/ModalLLM.astro';
import ModalBackup from '../components/modals/ModalBackup.astro';
import ModalCloud from '../components/modals/ModalCloud.astro';
import ProjectProtolab from '../components/modals/ProjectProtolab.astro';
import ProjectLLM from '../components/modals/ProjectLLM.astro';
import ProjectObservability from '../components/modals/ProjectObservability.astro';
import LiveLab from '../components/sections/LiveLab.astro';
import { getIconPath, getIconColor } from '../utils/icons';

// Préparer les icônes pour les modals
const iconWindows = getIconPath('windows');
const iconLinux = getIconPath('linux');
const iconPaloAlto = getIconPath('paloaltonetworks');
const iconProxmox = getIconPath('proxmox');
const iconGrafana = getIconPath('grafana');
const iconNvidia = getIconPath('nvidia');
const iconGit = getIconPath('git');
const iconDocker = getIconPath('docker');

const colorWindows = getIconColor('windows');
const colorLinux = getIconColor('linux');
const colorPaloAlto = getIconColor('paloaltonetworks');
const colorProxmox = getIconColor('proxmox');
const colorGrafana = getIconColor('grafana');
const colorNvidia = getIconColor('nvidia');
const colorGit = getIconColor('git');
const colorDocker = getIconColor('docker');
---

<BaseLayout
    title="Adrien Mercadier | TSSR - Portfolio"
    description="Portfolio de Adrien Mercadier - Technicien Supérieur Systèmes et Réseaux spécialisé en infrastructure, virtualisation et monitoring"
>
    <!-- Navigation -->
    <Navbar />
    <MobileMenu />

    <!-- Main Portfolio -->
    <div id="main-portfolio">
        <!-- Page Sections -->
        <Hero />
        <Skills />
        <Profile />
        <ProjectGallery />
        <LiveLab />
        <Documentation />
        <Contact />

        <!-- Project modals extracted to components -->
        <ProjectProtolab />
        <ProjectLLM />
        <ProjectObservability />

        <!-- Preview modals (hover) -->
        <PreviewWindows />
        <PreviewLinux />
        <PreviewPaloalto />
        <PreviewProxmox />
        <PreviewObservability />
        <PreviewLLM />
        <PreviewBackup />
        <PreviewCloud />

        <!-- Detail modals -->
        <ModalWindows />
        <ModalLinux />
        <ModalPaloalto />
        <ModalProxmox />
        <ModalObservability />
        <ModalLLM />
        <ModalBackup />
        <ModalCloud />

        <!-- ============================================
             PROJECT MODALS
             ============================================ -->
        <!-- Protolab Modal -->
        <div id="project-protolab" class="modal-overlay">
            <div class="modal-content modal-large">
                <div class="modal-header">
                    <h2 class="modal-title">
                        <div class="modal-title-icon"><svg viewBox="0 0 24 24"><path d="M21 16V8a2 2 0 0 0-1-1.73l-7-4a2 2 0 0 0-2 0l-7 4A2 2 0 0 0 3 8v8a2 2 0 0 0 1 1.73l7 4a2 2 0 0 0 2 0l7-4A2 2 0 0 0 21 16z"/></svg></div>
                        Infrastructure Protolab
                    </h2>
                    <button class="modal-close" aria-label="Fermer"><svg viewBox="0 0 24 24"><path d="M18 6L6 18M6 6l12 12"/></svg></button>
                </div>
                <div class="modal-body">
                    <div class="modal-section">
                        <h4 class="modal-section-title">Présentation</h4>
                        <p class="modal-text">Protolab est mon laboratoire personnel, une infrastructure complète simulant un environnement d'entreprise. Ce projet m'a permis de mettre en pratique l'ensemble de mes compétences TSSR dans un contexte réaliste.</p>
                    </div>
                    <div class="modal-section">
                        <h4 class="modal-section-title">Architecture Technique</h4>
                        <ul class="modal-list">
                            <li><strong>Hyperviseur :</strong> Proxmox VE 8.x sur serveur physique dédié (AMD Ryzen 7 5800X, 32GB RAM, RTX 3060)</li>
                            <li><strong>Firewall :</strong> Palo Alto PA-VM-50 avec 4 interfaces (WAN, LAN, DMZ, VPN)</li>
                            <li><strong>Active Directory :</strong> Windows Server 2022 (DC01) — Protolab.local</li>
                            <li><strong>Réseau :</strong> 3 VLANs segmentés (Production, Admin, DMZ) avec routing inter-VLAN</li>
                            <li><strong>Monitoring :</strong> Stack OpenTelemetry + Victoria Logs + Grafana</li>
                        </ul>
                    </div>
                    <div class="modal-section">
                        <h4 class="modal-section-title">Fonctionnalités Clés</h4>
                        <ul class="modal-list">
                            <li><strong>VPN GlobalProtect :</strong> Accès distant sécurisé avec authentification LDAP</li>
                            <li><strong>GPU Passthrough :</strong> RTX 3060 dédiée aux workloads IA (Ollama)</li>
                            <li><strong>Backup automatisé :</strong> Snapshots Proxmox + exports AD + config Palo Alto</li>
                            <li><strong>Zero Trust :</strong> Politiques de sécurité deny-by-default</li>
                        </ul>
                    </div>
                    <div class="modal-section">
                        <h4 class="modal-section-title">Stack Technique</h4>
                        <div class="modal-tags">
                            <span class="modal-tag">Proxmox VE</span>
                            <span class="modal-tag">Palo Alto PAN-OS</span>
                            <span class="modal-tag">Windows Server 2022</span>
                            <span class="modal-tag">Active Directory</span>
                            <span class="modal-tag">Docker</span>
                            <span class="modal-tag">Grafana</span>
                            <span class="modal-tag">OpenTelemetry</span>
                            <span class="modal-tag">Bash</span>
                            <span class="modal-tag">PowerShell</span>
                        </div>
                    </div>
                </div>
            </div>
        </div>

        <!-- LLM Modal -->
        <div id="project-llm" class="modal-overlay">
            <div class="modal-content modal-large">
                <div class="modal-header">
                    <h2 class="modal-title">
                        <div class="modal-title-icon modal-title-icon-magenta"><svg viewBox="0 0 24 24"><circle cx="12" cy="12" r="3"/><path d="M12 1v6M12 17v6M4.22 4.22l4.24 4.24M15.54 15.54l4.24 4.24M1 12h6M17 12h6"/></svg></div>
                        LLM Local + GPU
                    </h2>
                    <button class="modal-close" aria-label="Fermer"><svg viewBox="0 0 24 24"><path d="M18 6L6 18M6 6l12 12"/></svg></button>
                </div>
                <div class="modal-body">
                    <div class="modal-section">
                        <h4 class="modal-section-title">Présentation</h4>
                        <p class="modal-text">Déploiement d'une infrastructure IA locale permettant d'exécuter des modèles de langage (LLM) avec accélération GPU. Ce projet combine virtualisation avancée et technologies d'intelligence artificielle.</p>
                    </div>
                    <div class="modal-section">
                        <h4 class="modal-section-title">Architecture Technique</h4>
                        <ul class="modal-list">
                            <li><strong>GPU Passthrough :</strong> NVIDIA RTX 3060 12GB dédiée via IOMMU/VFIO</li>
                            <li><strong>Runtime :</strong> Ollama pour l'exécution des modèles LLM</li>
                            <li><strong>Interface :</strong> Open WebUI pour l'interaction utilisateur</li>
                            <li><strong>Routing :</strong> LiteLLM pour le load-balancing multi-modèles</li>
                            <li><strong>Conteneurisation :</strong> Docker avec nvidia-container-toolkit</li>
                        </ul>
                    </div>
                    <div class="modal-section">
                        <h4 class="modal-section-title">Modèles Déployés</h4>
                        <ul class="modal-list">
                            <li><strong>Llama 3.1 8B :</strong> Modèle généraliste pour conversations</li>
                            <li><strong>CodeLlama 7B :</strong> Assistance au développement</li>
                            <li><strong>Mistral 7B :</strong> Alternative performante</li>
                            <li><strong>Phi-3 :</strong> Modèle compact pour inférence rapide</li>
                        </ul>
                    </div>
                    <div class="modal-section">
                        <h4 class="modal-section-title">Défis Techniques Résolus</h4>
                        <ul class="modal-list">
                            <li>Configuration IOMMU et groupes de GPU dans Proxmox</li>
                            <li>Installation drivers NVIDIA dans environnement virtualisé</li>
                            <li>Optimisation mémoire VRAM pour modèles volumineux</li>
                            <li>Sécurisation de l'accès via reverse-proxy authentifié</li>
                        </ul>
                    </div>
                    <div class="modal-section">
                        <h4 class="modal-section-title">Stack Technique</h4>
                        <div class="modal-tags">
                            <span class="modal-tag">Ollama</span>
                            <span class="modal-tag">Open WebUI</span>
                            <span class="modal-tag">LiteLLM</span>
                            <span class="modal-tag">CUDA 12</span>
                            <span class="modal-tag">Docker</span>
                            <span class="modal-tag">NVIDIA Container Toolkit</span>
                            <span class="modal-tag">Proxmox GPU Passthrough</span>
                        </div>
                    </div>
                </div>
            </div>
        </div>

        <!-- Observability Modal -->
        <div id="project-observability" class="modal-overlay">
            <div class="modal-content modal-large">
                <div class="modal-header">
                    <h2 class="modal-title">
                        <div class="modal-title-icon modal-title-icon-green"><svg viewBox="0 0 24 24"><path d="M22 12h-4l-3 9L9 3l-3 9H2"/></svg></div>
                        Stack Observabilité
                    </h2>
                    <button class="modal-close" aria-label="Fermer"><svg viewBox="0 0 24 24"><path d="M18 6L6 18M6 6l12 12"/></svg></button>
                </div>
                <div class="modal-body">
                    <div class="modal-section">
                        <h4 class="modal-section-title">Présentation</h4>
                        <p class="modal-text">Pipeline de collecte et visualisation de logs centralisé, permettant une observabilité complète de l'infrastructure Protolab. Basé sur les standards CNCF avec OpenTelemetry.</p>
                    </div>
                    <div class="modal-section">
                        <h4 class="modal-section-title">Architecture du Pipeline</h4>
                        <ul class="modal-list">
                            <li><strong>Collecteur :</strong> OpenTelemetry Collector — point d'entrée unique</li>
                            <li><strong>Stockage :</strong> Victoria Logs — haute performance, faible empreinte</li>
                            <li><strong>Visualisation :</strong> Grafana — dashboards temps réel</li>
                            <li><strong>Alerting :</strong> Grafana Alerting avec notifications</li>
                        </ul>
                    </div>
                    <div class="modal-section">
                        <h4 class="modal-section-title">Sources de Données</h4>
                        <ul class="modal-list">
                            <li><strong>Palo Alto :</strong> Logs firewall via Syslog (traffic, threat, system)</li>
                            <li><strong>Windows Server :</strong> Event Logs (Security, Application, System)</li>
                            <li><strong>Linux :</strong> Journald + fichiers logs applicatifs</li>
                            <li><strong>Docker :</strong> Logs conteneurs via driver OTLP</li>
                        </ul>
                    </div>
                    <div class="modal-section">
                        <h4 class="modal-section-title">Dashboards Créés</h4>
                        <ul class="modal-list">
                            <li><strong>Security Overview :</strong> Tentatives d'intrusion, authentifications échouées</li>
                            <li><strong>Firewall Traffic :</strong> Flux réseau, top applications, menaces bloquées</li>
                            <li><strong>Infrastructure Health :</strong> État des VMs, ressources, uptime</li>
                            <li><strong>AD Audit :</strong> Connexions utilisateurs, modifications GPO</li>
                        </ul>
                    </div>
                    <div class="modal-section">
                        <h4 class="modal-section-title">Stack Technique</h4>
                        <div class="modal-tags">
                            <span class="modal-tag">OpenTelemetry</span>
                            <span class="modal-tag">Victoria Logs</span>
                            <span class="modal-tag">Grafana</span>
                            <span class="modal-tag">Syslog</span>
                            <span class="modal-tag">Docker Compose</span>
                            <span class="modal-tag">OTLP</span>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>

    <!-- Footer -->
    <Footer />
</BaseLayout>

<style is:global>
    #main-portfolio {
        opacity: 1;
    }

    /* Fix Hero positioning - make sure it starts at top of viewport */
    .hero {
        margin-top: 0 !important;
    }

    /* Ensure navigation doesn't push content down */
    .nav {
        position: fixed !important;
        top: 0 !important;
        z-index: 1000 !important;
    }

    /* Quand le menu mobile est ouvert, la navbar passe devant */
    body.menu-open .nav {
        z-index: 1003 !important;
    }

    /* Make sure Three.js canvas is behind and doesn't take space */
    #three-canvas {
        position: fixed !important;
        top: 0 !important;
        left: 0 !important;
        width: 100% !important;
        height: 100% !important;
        z-index: 0 !important;
        pointer-events: none !important;
    }

    /* Ensure main portfolio content is above canvas */
    #main-portfolio {
        position: relative;
        z-index: 1;
    }

    /* FORCE ALL ELEMENTS TO BE VISIBLE - Override all component opacity animations */
    .hero .status-badge,
    .hero .hero-title,
    .hero .hero-subtitle,
    .hero .typing-wrapper,
    .hero .cta-wrapper,
    .hero .scroll-indicator,
    .hero .stat-item,
    .section-label,
    .section-title,
    .section-desc,
    .skill-card,
    .project-card,
    .project-header,
    .project-content {
        opacity: 1 !important;
        transform: none !important;
        animation: none !important;
    }

    /* Profile Section */
    #profil {
        padding: 120px 40px;
        background: transparent;
    }

    .profile-grid {
        display: grid;
        grid-template-columns: 1fr 1fr;
        gap: 40px;
        align-items: stretch;
        max-width: 1400px;
        margin: 0 auto;
    }

    .profile-terminal-wrapper {
        background: rgba(10, 10, 10, 0.9);
        border: 1px solid var(--gray-mid);
        border-radius: 6px;
        box-shadow: 0 10px 30px rgba(0,0,0,0.5);
        overflow: hidden;
        transition: border-color 0.3s ease;
        display: flex;
        flex-direction: column;
    }

    .profile-terminal-wrapper:hover {
        border-color: var(--neon-cyan);
    }

    .terminal-header {
        background: var(--gray-dark);
        padding: 8px 12px;
        display: flex;
        align-items: center;
        gap: 8px;
    }

    .terminal-buttons { display: flex; gap: 5px; }
    .t-btn { width: 10px; height: 10px; border-radius: 50%; display: inline-block; }
    .t-btn.close { background: #ff5f56; }
    .t-btn.min { background: #ffbd2e; }
    .t-btn.max { background: #27c93f; }

    .terminal-title {
        flex-grow: 1;
        text-align: center;
        font-family: 'Space Mono', monospace;
        font-size: 12px;
        color: var(--gray-light);
    }

    .profile-terminal-body {
        padding: 15px;
        font-family: 'JetBrains Mono', monospace;
        font-size: 12px;
        line-height: 1.5;
        color: var(--gray-light);
        flex: 1;
        display: flex;
        flex-direction: column;
        justify-content: center;
    }

    .term-line {
        margin: 6px 0;
    }

    .term-prompt {
        color: var(--neon-green);
        margin-right: 6px;
    }

    .term-cmd {
        color: var(--neon-cyan);
    }

    .term-output {
        color: var(--white);
        margin: 4px 0 10px 0;
    }

    .term-text-block {
        margin: 10px 0;
        padding-left: 12px;
        border-left: 2px solid var(--gray-mid);
        color: var(--gray-lighter);
    }

    .term-text-block p {
        margin: 5px 0;
        font-size: 11px;
    }

    .highlight-magenta { color: var(--neon-magenta); }
    .text-neon-green { color: var(--neon-green); font-weight: bold; }

    .soft-skills-wrapper { padding: 0; }

    .soft-title {
        font-family: 'Bebas Neue', sans-serif;
        font-size: 28px;
        color: var(--white);
        margin-bottom: 20px;
        letter-spacing: 1px;
    }

    .blink { animation: blink 1s infinite; }

    @keyframes blink {
        0%, 100% { opacity: 1; }
        50% { opacity: 0; }
    }

    .soft-skill-item { margin-bottom: 16px; }

    .soft-info {
        display: flex;
        justify-content: space-between;
        margin-bottom: 6px;
        font-family: 'Space Mono', monospace;
        font-size: 11px;
        text-transform: uppercase;
    }

    .soft-label { color: var(--white); }
    .soft-val { color: var(--neon-cyan); }

    .soft-bar-bg {
        width: 100%;
        height: 8px;
        background: var(--gray-mid);
        position: relative;
        overflow: hidden;
        transform: skewX(-20deg);
    }

    .soft-bar-fill {
        height: 100%;
        transition: width 1s ease;
    }

    .soft-bar-fill::after {
        content: '';
        position: absolute;
        top: 0; right: 0; bottom: 0;
        width: 5px;
        background: rgba(255,255,255,0.8);
        box-shadow: 0 0 10px rgba(255,255,255,0.8);
    }

    .fill-cyan { background: var(--neon-cyan); box-shadow: 0 0 15px var(--neon-cyan); }
    .fill-magenta { background: var(--neon-magenta); box-shadow: 0 0 15px var(--neon-magenta); }
    .fill-green { background: var(--neon-green); box-shadow: 0 0 15px var(--neon-green); }
    .fill-yellow { background: var(--neon-yellow); box-shadow: 0 0 15px var(--neon-yellow); }

    .mini-stats-grid {
        display: flex;
        gap: 15px;
        margin-top: 30px;
    }

    .mini-stat {
        flex: 1;
        background: rgba(255,255,255,0.05);
        border: 1px solid rgba(255,255,255,0.1);
        padding: 15px;
        text-align: center;
        transition: 0.3s;
    }

    .mini-stat:hover {
        border-color: var(--neon-cyan);
        transform: translateY(-5px);
    }

    .ms-icon { font-size: 24px; display: block; margin-bottom: 8px; }
    .ms-label { font-family: 'Space Mono'; font-size: 11px; color: var(--gray-light); text-transform: uppercase; }

    /* Profile Section Responsive */
    @media (max-width: 900px) {
        .profile-grid {
            grid-template-columns: 1fr;
            gap: 3rem;
        }

        .soft-skills-wrapper {
            max-width: 100%;
        }
    }

    @media (max-width: 768px) {
        .profile-terminal-body {
            padding: 1.5rem;
        }

        .term-line {
            font-size: 0.75rem;
        }

        .soft-title {
            font-size: 1.25rem;
        }

        .soft-item-label {
            font-size: 0.75rem;
        }

        .soft-item-value {
            font-size: 0.75rem;
        }

        .mini-stats-grid {
            flex-direction: column;
            gap: 10px;
        }
    }

    /* Contact Section */
    .contact-section {
        padding: 120px 40px;
        background: transparent;
        text-align: center;
    }

    .contact-content {
        max-width: 600px;
        margin: 60px auto 0;
    }

    .contact-links {
        display: flex;
        flex-direction: column;
        gap: 20px;
    }

    .contact-link {
        display: flex;
        align-items: center;
        gap: 20px;
        padding: 20px;
        background: var(--gray-mid);
        border: 1px solid var(--gray-light);
        color: var(--white);
        text-decoration: none;
        transition: all 0.3s ease;
        font-family: 'JetBrains Mono', monospace;
    }

    .contact-link:hover {
        border-color: var(--neon-cyan);
        transform: translateX(10px);
        background: rgba(0, 255, 255, 0.05);
    }

    .contact-icon {
        width: 40px;
        height: 40px;
        display: flex;
        align-items: center;
        justify-content: center;
        background: var(--neon-cyan);
        color: var(--black);
        font-weight: bold;
        font-size: 20px;
    }

    /* Footer */
    .footer {
        padding: 40px 20px;
        background: var(--black);
        border-top: 1px solid var(--gray-mid);
    }

    .footer-inner {
        max-width: 1200px;
        margin: 0 auto;
        display: flex;
        justify-content: space-between;
        align-items: center;
        font-family: 'JetBrains Mono', monospace;
        font-size: 14px;
    }

    .footer-text {
        color: var(--gray-light);
    }

    .footer-separator {
        color: var(--gray-mid);
        margin: 0 15px;
    }

    .footer-left {
        display: flex;
        align-items: center;
    }

    /* Back to Top */
    .back-to-top {
        position: fixed;
        bottom: 40px;
        right: 40px;
        width: 50px;
        height: 50px;
        background: var(--neon-cyan);
        color: var(--black);
        border: none;
        cursor: pointer;
        font-size: 24px;
        font-weight: bold;
        opacity: 0;
        transform: translateY(20px);
        transition: all 0.3s ease;
        z-index: 1000;
        pointer-events: none;
    }

    .back-to-top.visible {
        opacity: 1;
        transform: translateY(0);
        pointer-events: all;
    }

    .back-to-top:hover {
        background: var(--neon-magenta);
    }

    /* Modal Styles */
    .modal-overlay {
        position: fixed;
        top: 0;
        left: 0;
        width: 100%;
        height: 100%;
        background: rgba(0, 0, 0, 0.9);
        z-index: 10000;
        display: none;
        align-items: center;
        justify-content: center;
        backdrop-filter: blur(10px);
    }

    .modal-overlay.active {
        display: flex;
    }

    .modal-content {
        background: var(--gray-dark);
        border: 2px solid var(--neon-cyan);
        max-width: 800px;
        max-height: 80vh;
        overflow-y: auto;
        position: relative;
        padding: 40px;
    }

    .modal-header {
        display: flex;
        justify-content: space-between;
        align-items: center;
        margin-bottom: 30px;
        border-bottom: 1px solid var(--gray-mid);
        padding-bottom: 20px;
    }

    .modal-title {
        font-family: 'Bebas Neue', sans-serif;
        font-size: 32px;
        color: var(--neon-cyan);
    }

    .modal-close {
        background: transparent;
        border: 1px solid var(--gray-light);
        color: var(--white);
        width: 40px;
        height: 40px;
        font-size: 24px;
        cursor: pointer;
        transition: all 0.3s ease;
    }

    .modal-close:hover {
        border-color: var(--neon-magenta);
        color: var(--neon-magenta);
    }

    .modal-body h4 {
        color: var(--neon-cyan);
        font-size: 20px;
        margin: 20px 0 15px;
        font-family: 'Space Mono', monospace;
    }

    .modal-body ul {
        list-style: none;
        padding-left: 0;
    }

    .modal-body li {
        color: var(--gray-light);
        margin-bottom: 10px;
        padding-left: 20px;
        position: relative;
    }

    .modal-body li:before {
        content: "▸";
        color: var(--neon-green);
        position: absolute;
        left: 0;
    }

    @media (max-width: 768px) {
        .footer-inner {
            flex-direction: column;
            gap: 15px;
            text-align: center;
        }

        .footer-separator {
            display: none;
        }

        .contact-section {
            padding: 80px 20px;
        }

        .modal-content {
            margin: 20px;
            padding: 20px;
            max-height: 90vh;
        }
    }
</style>

<script>
    // Scroll to top on page load
    document.addEventListener('DOMContentLoaded', () => {
        window.scrollTo({ top: 0, behavior: 'instant' });
    });
</script>
